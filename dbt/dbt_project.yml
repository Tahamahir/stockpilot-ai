name: stockpilot
version: "1.0.0"
config-version: 2

profile: stockpilot

model-paths:
  - models

analysis-paths:
  - analyses

test-paths:
  - tests

macro-paths:
  - macros

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  stockpilot:
    staging:
      +materialized: view

    intermediate:
      +materialized: view

    marts:
      +materialized: table